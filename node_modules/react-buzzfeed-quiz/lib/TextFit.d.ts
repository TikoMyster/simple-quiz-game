import { ReactNode, CSSProperties } from "react";
import { FC } from "react";
interface TextFitProps {
    capAt: number;
    min: number;
    max: number;
    className?: string;
    children: ReactNode;
    style: CSSProperties;
    outerContainerWidth?: number;
    gridLayout?: boolean;
    question?: boolean;
}
declare const TextFit: FC<TextFitProps>;
export default TextFit;
